{
  "HOST_NAME": "{{ host_name }}",
  "SERVICE_NAME": "{{ service_name }}",
  "TCP_VERSION": "{{ service_version }}",
  "HTTP_VERSION": "{{ service_version }}",
  "REGISTRY_HOST": "{{ registry_host }}",
  "REGISTRY_PORT": "{{ registry_port }}",
  "REDIS_HOST": "{{ redis_host }}",
  "REDIS_PORT": "{{ redis_port }}",
  "HTTP_HOST": "{{ http_host }}",
  "TCP_HOST": "{{ tcp_host }}",
  "HTTP_PORT": "{{ http_port }}",
  "TCP_PORT": "{{ tcp_port }}",
  "SIGNALS": "{{ signals }}",
  "DATABASE_SETTINGS": {
    "database": "{{ database_name }}",
    "user": "{{ database_user }}",
    "password": "{{ database_password }}",
    "host": "{{ database_host }}",
    "port": "{{ database_port }}"
  }
}