server {
    listen      80;
    charset     utf-8;

    client_max_body_size 75M;

    {% for service in services %}

    location /{{ service.service_uri }}/v{{ service.service_version }}/ {
        proxy_pass http://{{ service.ip_address }}:{{ service.http_port }};
    }
    {% endfor %}

}

